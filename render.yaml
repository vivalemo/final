services:
  - type: web
    name: oxapay-webhook
    runtime: python
    buildCommand: ""
    startCommand: uvicorn webhook:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PORT
        value: 10000
